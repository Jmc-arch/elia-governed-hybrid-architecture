# ELIA Phase 0 — Dependencies
# Intentionally minimal. No neural libraries at this stage.

# No external dependencies required for Phase 0.
# All modules use Python standard library only:
#   - asyncio  (message bus)
#   - sqlite3  (memory layer)
#   - threading (state coordination locks)
#   - json, uuid, dataclasses, datetime

# Phase 1+ will introduce:
# pydantic>=2.0
# loguru

# Phase 3 (optional neural) will introduce:
# torch
# transformers
# sentence-transformers
